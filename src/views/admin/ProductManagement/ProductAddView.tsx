import { ProductAddContainer } from '@/components/products';

/**
 * Renders "ProductAdd" view
 * url: /admin/products/add
 * @page ProductAdd
 */
const ProductAddView = () => {
  return <ProductAddContainer />;
};

export default ProductAddView;
