import { OrderManagementTable } from '@/components/orders';

/**
 * Renders "OrderManagement" view
 * url: /admin/orders
 * @page ProductManagement
 */
const OrderManagementView = () => {
  return <OrderManagementTable />;
};

export default OrderManagementView;
